### install uv (UltraViolet)


#### In Ubuntu
```bash
sudo apt install curl
curl -LsSf https://astral.sh/uv/install.sh | sudo sh
uv version
```

#### In Windows
```bash

```

#### In MacOS
```bash
brew install uv
```
